name: Deploy to cPanel

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    name: Build & Deploy
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup PHP 8.4
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          extensions: mbstring, bcmath, pdo_mysql, pdo_sqlite, zip, gd, curl, xml
          coverage: none

      - name: Install Composer dependencies
        run: composer install --no-dev --optimize-autoloader --no-interaction --prefer-dist

      - name: Setup Node.js 20
        uses: actions/setup-node@v5
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install NPM dependencies and build assets
        run: |
          npm ci
          npm run build
          rm -f public/hot

      - name: Configure SSH
        run: |
          mkdir -p ~/.ssh
          printf "%s" "${{ secrets.CPANEL_SSH_KEY }}" | tr -d '\r' > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -p "${{ secrets.CPANEL_PORT }}" -H "${{ secrets.CPANEL_HOST }}" >> ~/.ssh/known_hosts || true
          cat >> ~/.ssh/config << EOF
          Host deploy_target
            HostName ${{ secrets.CPANEL_HOST }}
            Port ${{ secrets.CPANEL_PORT }}
            User ${{ secrets.CPANEL_USER }}
            IdentityFile ~/.ssh/deploy_key
            UserKnownHostsFile ~/.ssh/known_hosts
            StrictHostKeyChecking yes
          EOF

      - name: Test SSH connection
        run: |
          ssh -o ConnectTimeout=20 deploy_target "echo Connected to \\$(hostname) as \\$(whoami)"

      - name: Sync files to server
        run: |
          rsync -az --delete \
            --exclude='.git' \
            --exclude='.github' \
            --exclude='node_modules' \
            --exclude='.env' \
            --exclude='.env.*' \
            --exclude='storage/logs/*.log' \
            --exclude='storage/framework/cache/data' \
            --exclude='storage/framework/sessions/*' \
            --exclude='storage/framework/views/*' \
            --exclude='tests/' \
            --exclude='phpunit.xml' \
            --exclude='*.md' \
            ./ "deploy_target:${{ secrets.CPANEL_APP_PATH }}/"

      - name: Run post-deploy commands
        run: |
          ssh deploy_target "
            set -e
            cd '${{ secrets.CPANEL_APP_PATH }}'
            mkdir -p storage/framework/{cache,sessions,views} storage/logs bootstrap/cache
            chmod -R ug+rwx storage bootstrap/cache || true
            php artisan migrate --force
            php artisan optimize:clear
            php artisan storage:link --force
            php artisan optimize
            echo 'Deployment complete!'
          "
